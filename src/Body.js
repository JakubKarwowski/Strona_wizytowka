import React from 'react';
import './Body.scss'
import {Route} from 'react-router-dom'

class Body extends React.Component {
    render() {
        return (  <h1>Body</h1>

        );
    };
};

export default Body;